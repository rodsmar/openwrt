--- a/net/mac80211/rx.c
+++ b/net/mac80211/rx.c
@@ -3464,6 +3464,9 @@ ieee80211_process_rx_twt_action(struct i
 	if (sdata->vif.type != NL80211_IFTYPE_AP)
 		return false;
 
+	if (!sdata->vif.bss_conf.twt_responder)
+		return false;
+
 	if (!rx->local->ops->add_twt_setup)
 		return false;
 
